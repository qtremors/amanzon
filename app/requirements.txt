# Requirements for deployment (generated from pyproject.toml)
# Use `uv pip compile pyproject.toml > requirements.txt` to regenerate

django>=5.2.9
pillow>=12.0.0
python-dotenv>=1.2.1
razorpay>=2.0.0
